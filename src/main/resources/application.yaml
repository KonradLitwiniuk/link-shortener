spring:
  application:
    name: link-shortener
  datasource:
    url: "jdbc:postgresql://localhost:5432/shortener"
    username: "dev"
    password: "dev"
  jpa:
    hibernate.ddl-auto: "update"
    open-in-view: false
app:
  base-url: "http://localhost:8080/"