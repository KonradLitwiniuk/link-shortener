spring:
  application:
    name: link-shortener
  datasource:
    url: "jdbc:postgresql://localhost:5432/shortener"
    username: "dev"
    password: "dev"
  jpa:
      hibernate.ddl-auto: "update"
      show-sql: true
      properties:
        hibernate:
          format_sql: true
app:
  base-url: "http://localhost:8080/"